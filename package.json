{
  "name": "reminder-bot",
  "version": "1.0.0",
  "description": "A Telegram bot for project reminders using Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "moment-jalaali": "^0.10.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 